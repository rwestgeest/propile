<%= title "Editing session" %>

<div class="alignright">
  Created: <%= l @session.created_at %> -- 
  Modified: <%= l @session.updated_at %> 
</div>

<%= render :partial => 'form', :locals => { :session => "@session", :editable_presenters=> "false" }  %>

<div class="full"> 
  <%= link_to 'Show', @session %> |
  <%= link_to 'Back', sessions_path %>
</div>
