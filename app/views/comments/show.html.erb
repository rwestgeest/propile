<%= render 'sessions/show_short', :session => @comment.review.session  %>
<hr>

<%= render 'reviews/show', :review => @comment.review  %>

<%= render 'comments/show', :comment => @comment  %>

<%= guarded_link_to 'Edit', controller: 'comments', action: 'edit', id: @comment %> |
<%= link_to 'Back to Session', @comment.review.session %> 
