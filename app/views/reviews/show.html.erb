<%= title "Review for: #{@review.session.title}" %> 
<%= content_for :sub_menu do %> 
  <%= guarded_link_to 'Edit', controller: 'reviews', action: 'edit', id: @review  %> &nbsp; | &nbsp;
  <%= link_to 'Back to Session', @review.session %>
<% end %>
<%= render 'sessions/show_short', :session => @review.session  %>
<hr />

<%= render 'reviews/show', :review => @review  %>

